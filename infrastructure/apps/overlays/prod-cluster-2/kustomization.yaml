---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../prod
patches:
  - target:
      kind: HelmRelease
      name: kubeping
    patch: |
      - op: replace
        path: /spec/values/web/ingress/host
        value: kubeping.prod-cluster-2.example.az
